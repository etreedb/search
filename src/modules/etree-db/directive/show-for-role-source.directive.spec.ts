import { ShowForRoleSourceDirective } from './show-for-role-source.directive';

describe('ShowForRoleSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForRoleSourceDirective();
    expect(directive).toBeTruthy();
  });
});
