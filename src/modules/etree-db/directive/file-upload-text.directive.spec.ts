import { FileUploadTextDirective } from './file-upload-text.directive';

describe('FileUploadTextDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadTextDirective();
    expect(directive).toBeTruthy();
  });
});
