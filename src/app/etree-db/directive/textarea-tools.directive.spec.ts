import { TextareaToolsDirective } from './textarea-tools.directive';

describe('TextareaToolsDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaToolsDirective();
    expect(directive).toBeTruthy();
  });
});
