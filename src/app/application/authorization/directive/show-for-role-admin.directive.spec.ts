import { ShowForRoleAdminDirective } from './show-for-role-admin.directive';

describe('ShowForRoleAdminDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForRoleAdminDirective();
    expect(directive).toBeTruthy();
  });
});
